import React from "react";
import { Typography } from "@mui/material";
import Layout from "./Layout";

const Settings = () => {
  return (
    <Layout componentName={"Settings"}>
      <Typography ml={4}>Settings component</Typography>
    </Layout>
  );
};

export default Settings;
