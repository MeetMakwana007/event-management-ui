import React from "react";
import { Typography } from "@mui/material";
import Layout from "./Layout";

const Account = () => {
  return (
    <Layout componentName={"Account"}>
      <Typography ml={4}>Account component</Typography>
    </Layout>
  );
};

export default Account;
